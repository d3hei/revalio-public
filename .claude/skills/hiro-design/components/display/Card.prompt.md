Flat bordered content/product card — hairline border, small radius, subtle hover lift.

```jsx
<Card eyebrow="Stacks" title="Chainhook" arrow interactive>
  Build smarter apps with webhook-like triggers that react to onchain events in real time.
</Card>
```

Props: `eyebrow`, `title`, `arrow`, `interactive` (hover lift), `padding`.
