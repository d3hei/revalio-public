Text input with an uppercase mono label, hairline border and violet focus ring.

```jsx
<Input label="Email" placeholder="you@domain.com" hint="We won't spam you." />
<Input label="Contract" prefix="$" mono error="Invalid principal" />
```

Props: `label`, `prefix` (mono glyph), `hint`, `error`, `mono`, `disabled`.
